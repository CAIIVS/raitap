# Scaffolding only — see issue #132. ``acas_xu_inputs`` / ``acas_xu_labels``
# sample sources are NOT yet registered in ``raitap.data.samples.SAMPLE_SOURCES``;
# point ``source:`` / ``labels.source:`` at a local path until those fixtures land.
# The Marabou unit + e2e tests bypass this YAML and load the ACAS Xu net directly
# from the registered ``acas_xu_n1_1`` sample.
name: acas_xu
description: "ACAS Xu collision-avoidance MLP (5 inputs -> 5 outputs); VNN-COMP fixture"
source: acas_xu_inputs  # placeholder — replace with real path or wait for follow-up.
input_metadata:
  kind: tabular
  feature_names: [rho, theta, psi, v_own, v_int]
labels:
  source: acas_xu_labels  # placeholder — replace with real path or wait for follow-up.
  column: label
  encoding: index
