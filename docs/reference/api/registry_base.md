# `raitap.registry_base`

Cross-module interface declared by every adapter that publishes an
algorithm-name → typed-hints registry. Used by both the transparency
explainer adapters and the robustness assessor adapters.

```{eval-rst}
.. autoclass:: raitap.registry_base.WithAlgorithmRegistry
   :members:
   :show-inheritance:
```
