---
title: "Examples"
description: "Minimal examples to demonstrate the config syntax. For overall config syntax see {doc}../configuration/general; for the programmatic API see {doc}../configuration/python-api."
myst:
  html_meta:
    "description": "Minimal examples to demonstrate the config syntax. For overall config syntax see {doc}../configuration/general; for the programmatic API see {doc}../configuration/python-api."
---

# Examples

Minimal examples to demonstrate the config syntax. For overall config syntax see {doc}`../configuration/general`; for the programmatic API see {doc}`../configuration/python-api`.

## Recipes

- {doc}`imagenet-captum-ig-pgd` — single explainer + single assessor (baseline shape).
- {doc}`multi-explainer` — two transparency explainers running side by side.
- {doc}`multi-visualiser` — one explainer rendered through two visualiser configs.
- {doc}`multi-assessor` — two robustness assessors compared in one report.

```{toctree}
:hidden:
:maxdepth: 1

imagenet-captum-ig-pgd
multi-explainer
multi-visualiser
multi-assessor
```
