---
title: "Using RAITAP in your project"
description: "Manage RAITAP's dependencies in your own project: automatic inference (default) or manual control via --custom-deps. Covers per-hardware index routing for torch-cuda / torch-intel and the triton-xpu pre-release knob."
myst:
  html_meta:
    "description": "Manage RAITAP's dependencies in your own project: automatic inference (default) or manual control via --custom-deps. Covers per-hardware index routing for torch-cuda / torch-intel and the triton-xpu pre-release knob."
---

(consuming-raitap)=

# Using RAITAP in your project

This section details how set up your project file to use RAITAP in an existing project on various hardware (CUDA, Intel...).

```{toctree}
:maxdepth: 1

automatic
manual
```
