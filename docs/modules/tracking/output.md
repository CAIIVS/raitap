---
title: "Output"
description: "Tracking does not create any additional output; it simply forwards the existing one (as defined in {doc}../../using-raitap/understanding-outputs) to the tracking backend in a way the latter can understand."
myst:
  html_meta:
    "description": "Tracking does not create any additional output; it simply forwards the existing one (as defined in {doc}../../using-raitap/understanding-outputs) to the tracking backend in a way the latter can understand."
---

# Output

Tracking does not create any additional output; it simply forwards the existing one (as defined in {doc}`../../using-raitap/understanding-outputs`) to the tracking backend in a way the latter can understand.
